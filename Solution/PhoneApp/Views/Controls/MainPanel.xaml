<?xml version="1.0" encoding="utf-8" ?>
<ContentView xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:toolkit="http://schemas.microsoft.com/dotnet/2022/maui/toolkit"
             xmlns:f="clr-namespace:Fonts"
             xmlns:m="clr-namespace:MicroVue.Models"
             xmlns:vm="clr-namespace:MicroVue.ViewModels"
             xmlns:v="clr-namespace:MicroVue.Views"
             x:Class="MicroVue.Views.MainPanel"
             x:DataType="vm:AnalysisViewModel"
             x:Name="mainPanel">

    <ContentView.Resources>
        <toolkit:InvertedBoolConverter x:Key="InvertedBoolConverter" />
    </ContentView.Resources>

    <Grid RowDefinitions="*" ColumnDefinitions="*">
        
        <!--Regions-->
        <Grid Grid.Row="0" RowDefinitions="Auto, *, Auto" ColumnDefinitions="*" Margin="5"
              IsVisible="{Binding IsRegionSelected, Converter={StaticResource InvertedBoolConverter}}">

            <ScrollView Grid.Row="1" Margin="5">

                <CollectionView ItemsSource="{Binding Scene.Regions}" SelectedItem="{Binding SelectedRegion}" SelectionMode="Single">
                    <CollectionView.ItemTemplate>
                        <DataTemplate x:DataType="m:Region">
                            <Border StrokeThickness="1" Stroke="Gray" Margin="10, 1" Padding="5">
                                <Grid ColumnDefinitions="Auto, Auto">
                                    <Label Grid.Column="0" Text="{Binding Name}" VerticalOptions="Center" Margin="5"/>
                                    <Rectangle Grid.Column="1"  VerticalOptions="Center" Margin="10, 0, 10, 0"
                                            Fill="Transparent" Stroke="{Binding Color}" StrokeThickness="3" 
                                            WidthRequest="20" HeightRequest="20" />
                                </Grid>
                            </Border>
                        </DataTemplate>
                    </CollectionView.ItemTemplate>
                </CollectionView>

            </ScrollView>
            
            <Grid Grid.Row="2" ColumnDefinitions="Auto, *, *">

                <!--<Label Grid.Column="0" Text=" Add Regions:" VerticalOptions="Center" Margin="5"/>-->

                <Button Grid.Column="1" Text="+ Target" VerticalOptions="Center" Margin="5" WidthRequest="120"
                        Command="{Binding AddTargetCommand}"/>

                <Button Grid.Column="2" Text="+ Background" VerticalOptions="Center" Margin="5" WidthRequest="120"
                        Command="{Binding AddBackgroundCommand}"/>

            </Grid>
        </Grid>
        
        <!--Selected region-->
        <Grid Grid.Row="0" RowDefinitions="Auto, Auto, Auto, Auto, Auto, Auto, *" ColumnDefinitions="Auto, *, Auto" Margin="5"
              IsVisible="{Binding IsRegionSelected}">

            <Button Grid.Row="0" Grid.Column="0" Text="{x:Static f:FluentUI.arrow_left_24_regular}" FontSize="22"
                    FontFamily="{x:Static f:FluentUI.FontFamily}" Margin="5"
                    Command="{Binding ShowRegionsCommand}"/>

            <Button Grid.Row="0" Grid.Column="2" Text="{x:Static f:FluentUI.delete_32_regular}" Margin="5"
                    FontSize="22" FontFamily="{x:Static f:FluentUI.FontFamily}" TextColor="White" BackgroundColor="Red"
                    Command="{Binding DeleteCommand}" CommandParameter="{Binding SelectedRegion}"/>

            <Label Grid.Row="1" Grid.Column="0" Text="Name:" VerticalOptions="Center" Margin="5"/>
            <Label Grid.Row="1" Grid.Column="1" Text="{Binding SelectedRegion.Name}" VerticalOptions="Center" Margin="5"/>

            <Label Grid.Row="2" Grid.Column="0" Text="Color:" VerticalOptions="Center" Margin="5"/>
            <Label Grid.Row="2" Grid.Column="1" Text="{Binding SelectedRegion.ColorText}" 
                   BackgroundColor="{Binding SelectedRegion.Color}" VerticalOptions="Center" Margin="5"/>
            
            <Label Grid.Row="3" Grid.Column="0" Text="Size:" VerticalOptions="Center" Margin="5"/>
            <Slider Grid.Row="3" Grid.Column="1" Value="{Binding SelectedRegion.Size}" VerticalOptions="Center" Margin="5"
                Minimum="0" Maximum="2000"/>

            <Label Grid.Row="4" Grid.Column="0" Text="X:" VerticalOptions="Center" Margin="5"/>
            <Slider Grid.Row="4" Grid.Column="1" Value="{Binding SelectedRegion.X}" VerticalOptions="Center" Margin="5"
                Minimum="0" Maximum="{Binding MediaWidth}"/>

            <Label Grid.Row="5" Grid.Column="0" Text="Y:" VerticalOptions="Center" Margin="5"/>
            <Slider Grid.Row="5" Grid.Column="1" Value="{Binding SelectedRegion.Y}" VerticalOptions="Center" Margin="5"
                Minimum="0" Maximum="{Binding MediaHeight}"/>


        </Grid>
        
    </Grid>

</ContentView>
